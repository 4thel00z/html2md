# html2md

Markdown → self-contained HTML (Tailwind CSS v4 + Typography + DaisyUI themes).

## Install

```bash
bun install
```

## Library usage

```ts
import { convertToHtml } from "html2md";

const html = await convertToHtml("# Hello");
```

## Demo app (local)

```bash
bun run start
```

